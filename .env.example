# Apollo Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Required for AI Features
# =============================================================================

# Anthropic API key (required for /interpret, /generate, /refine endpoints)
ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# Optional AI Configuration
# =============================================================================

# Model to use (default: claude-sonnet-4-20250514)
# APOLLO_AI_MODEL=claude-sonnet-4-20250514

# Max tokens for AI responses (default: 4096)
# APOLLO_AI_MAX_TOKENS=4096

# Temperature for AI responses (default: 0.7)
# APOLLO_AI_TEMPERATURE=0.7

# Max retries on rate limit/server errors (default: 3)
# APOLLO_AI_MAX_RETRIES=3

# =============================================================================
# Server Configuration
# =============================================================================

# API server port (default: 3000)
# PORT=3000

# Data directory for story storage (default: ~/.apollo)
# APOLLO_DATA_DIR=/path/to/data
